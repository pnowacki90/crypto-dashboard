%h1 Cryptos
%div.table-responsive
  %table.table.table-hover.table-condensed
    %thead
      %tr
        %th Name
        %th Symbol
        %th Cost per
        %th Amount owned
        %th{:colspan => 3}
    %tbody
      - @cryptos.each do |crypto|
        - if crypto.user_id == current_user.id
          - if crypto.symbol
            - crypto.symbol = crypto.symbol.upcase
          - for x in @coins
            - if crypto.symbol == x["symbol"]
              %tr
                %td= x["name"]
                %td= crypto.symbol
                %td= crypto.cost_per
                %td= crypto.amount_owned
                %td= link_to 'Show', crypto
                %td= link_to 'Edit', edit_crypto_path(crypto)
                %td= link_to 'Destroy', crypto, method: :delete, data: { confirm: 'Are you sure?' }
<br />
= link_to 'New Crypto', new_crypto_path, class: "btn btn-primary"
